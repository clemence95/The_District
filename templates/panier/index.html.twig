{# templates/panier/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Votre Panier</h1>

    {% if panier is empty %}
        <p>Le panier est vide.</p>
    {% else %}
        <ul>
            {% for plat in panier %}
                <li>
                    {{ plat.nom }} - {{ plat.prix }} €
                    <a href="{{ path('retirer_du_panier', {'plat': plat.id}) }}">Retirer du panier</a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}


