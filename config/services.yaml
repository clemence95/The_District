# Ce fichier est le point d'entrée pour configurer vos propres services.
# Les fichiers dans le sous-répertoire packages/ configureront vos dépendances.

# Placez ici les paramètres qui ne changent pas sur chaque machine où l'application est déployée
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # Configuration par défaut pour les services dans *ce* fichier
    _defaults:
        autowire: true      # Injecte automatiquement les dépendances dans vos services.
        autoconfigure: true # Enregistre automatiquement vos services en tant que commandes, écouteurs d'événements, etc.

# Rend les classes dans src/ disponibles pour être utilisées en tant que services
# cela crée un service par classe dont l'identifiant est le nom de classe entièrement qualifié
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

# Ajoutez plus de définitions de services lorsque une configuration explicite est nécessaire
# veuillez noter que les dernières définitions remplacent toujours les précédentes

# Définition de services spécifiques pour les repositories



